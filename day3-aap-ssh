#in the terminal run this command which will generate the ssh key
 mkdir ssh-keys                                                                                                        
  cd .\ssh-keys\ 
  #just press enter enter dont give any passpharses
  ssh-keygen -m PEM -t rsa -b 4096 -C "azureuser@myserver" -f terraform-azure.pem                                       
  cd .. 
  ###once generate you can see it in your visual studio code

 ###inside your terraform project folder create a folder app inside the folder create a file app.sh #inside the file copy from line 10 till 14
#!/bin/bash
sudo apt-get update -y
sudo apt-get install -y unzip wget curl git
sudo apt-get install -y apache2
sudo echo "welcome to my website $(hostname)" > /var/www/html/index.html
